<!-- BOILERPLATE_PLACEHOLDER / LEGACY_OPTIONAL: prefer PROGRESS.md NOW as the single next-task pointer. See BOILERPLATE.md -->

# Next task

**Bootstrap this run from the active objective**

- Read the objective the human pasted into the agent. Do **not** invent a genre that is not in that objective.
- Rewrite **PROGRESS.md** / **CONTENT.md** (and README playable sections) for the real first milestone.
- Replace BOILERPLATE_TOY `src/` / `index.html` with the game. Do **not** edit BOILERPLATE_OWNED paths.
- `.autonomy/*` is optional — do not use it as the primary tracker.
- Keep `npm run gate` green and Vite `base` = `/autonomous-lab/`.

ACCEPT:

- [ ] PROGRESS.md NOW points at the first real milestone (not boilerplate placeholders)
- [ ] `npm run gate` passes
- [ ] Toy economy UI gone or clearly superseded by the game shell
