# Roadmap (fill per run)

Ordered milestones for **this** autonomy run only. Derive from the active objective; keep `TASKS/next.md` on exactly one slice.

1. **Bootstrap from objective** — Write SPEC/DECISIONS/CONTENT from the objective; leave Vite `base` and gate intact.
2. **…** — Replace with objective milestones (M1, M2, …).

ACCEPT for any slice: objective ACCEPT met + `npm run gate` green + PROGRESS updated.
