# Roadmap (fill per run)

Ordered milestones for **this** autonomy run only. Derive from the active prompt; keep `TASKS/next.md` on exactly one slice.

1. **Bootstrap from objective** — Write SPEC/DECISIONS/CONTENT from the prompt; leave Vite `base` and gate intact.
2. **…** — Replace with prompt milestones (M1, M2, …).

ACCEPT for any slice: prompt ACCEPT met + `npm run gate` green + PROGRESS updated.
