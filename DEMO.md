# Demo — Phase 3 storyboard

Storyboard steps 0–8 per overnight law (title → Play → clicks → generators → upgrade →
layer switch → Ascend with Harmonics visible → layer 2–3 UI → hold ≥2s).
Recording: `demo/record.mjs` (Playwright) → `demo/demo.webm` + `demo/frames/<step>.png`.

Status: not started (Phase 3; requires Phase 2b green first).

## Visual validation
_(frames + read_image PASS/FAIL recorded here; file size / pixel dumps are NOT evidence)_
