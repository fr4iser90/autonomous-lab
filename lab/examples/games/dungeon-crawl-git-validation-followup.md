================================================================
RESUME — git / CI validator (do not code the game)
================================================================

You are resuming the **Ashen Delve git/CI validator**
(`dungeon-crawl-git-validation.md`). No game features.

1. `git fetch` — identify `agent/dungeon-crawl-*` from PROGRESS / open PR.
2. Read BUGS.md ## Open (dedupe).
3. Re-open `dungeon-crawl-git-validation.md` for checklist + FORBIDDEN.
4. create_goal if useful; policy error → IGNORE; continue with tools.
5. Check branch / PR / `gate` / Actions queue / dual-branch smell → append BUGS.
6. Always leave a next tool call. Never mark create_goal complete.
